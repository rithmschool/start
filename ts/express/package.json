{
  "name": "express",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node server.js",
    "test": "vitest",
    "coverage": "vitest --coverage",
    "debug": "node --inspect-brk server.js",
    "debug:test": "node --inspect-brk $(which vitest)"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.16",
    "@tsconfig/node21": "^21.0.1",
    "@types/supertest": "^6.0.2",
    "supertest": "^6.3.4",
    "vitest": "^1.2.2"
  },
  "dependencies": {
    "express": "^5.0.0-beta.1"
  }
}
