{
  "name": "rithmschool-start-ts-node",
  "version": "1.0.0",
  "homepage": "https://github.com/rithmschool/start/",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "start": "node $npm_package_main",
    "start:debug": "node --inspect-brk $npm_package_main",
    "test": "vitest ts",
    "test:cov": "vitest ts --coverage",
    "test:debug": "vitest ts --run --inspect-brk --no-file-parallelism"
  },
  "devDependencies": {
    "@types/node": "^20.11.16",
    "vitest": "^1.2.2"
  }
}
